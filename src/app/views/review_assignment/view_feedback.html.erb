

<p>This is the feedback given by your peers</p>
<% if @past_due %>
	<% @questions.each do |question| %>
		<h2><%= question.question_title %></h2>
		<p><%= question.content %></p>
		<ul>
			<% @answers[question.id].each do |answer| %>
			<li> <%= answer.answer %> </li>
			<% end %>
		</ul>
		</br>
	<% end %>
	
	<h3> Faculty Review Comments <h3>
	<%= 
	
<% else %>
	<h2>The assignment is not over yet. You can not see info for an assignment that is not past due </h2>
<% end %>
